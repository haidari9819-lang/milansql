{{- if and .Values.persistence.enabled (eq .Values.replicaCount 1.0) }}
# Standalone PVC for single-instance deployments
# (StatefulSet uses volumeClaimTemplates instead for replicaCount > 1)
{{- end }}
# Note: PVCs are managed via StatefulSet volumeClaimTemplates in deployment.yaml
# This file documents the PVC spec for reference.
#
# To manually create a PVC:
# kubectl apply -f - <<EOF
# apiVersion: v1
# kind: PersistentVolumeClaim
# metadata:
#   name: milansql-data
# spec:
#   accessModes: [ReadWriteOnce]
#   resources:
#     requests:
#       storage: {{ .Values.persistence.size }}
# EOF
